---
phase: 01-broken-foundation-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [css/pwg.css]
autonomous: true
gap_closure: true
requirements: [INFRA-01]
must_haves:
  truths:
    - "Body text renders in Lato, not Helvetica/Arial, on all pages"
    - "Google Fonts downloads the Lato binary files (visible in DevTools Network > Font)"
  artifacts:
    - path: "css/pwg.css"
      provides: "font-family override on body element"
      contains: "font-family: 'Lato', sans-serif"
  key_links:
    - from: "_includes/head.html"
      to: "css/pwg.css"
      via: "Google Fonts loads Lato; pwg.css body rule applies it"
      pattern: "font-family.*Lato"
---

<objective>
Apply the already-imported Lato font to the body element in pwg.css so the browser
actually uses the Google Fonts download rather than falling back to Bootstrap's
Helvetica stack.

Purpose: Phase 1 UAT test 2 failed — user observed only "Google Sans" (a system font)
because no CSS rule activated the Lato font family loaded by the Google Fonts link
tag in head.html. The font files are imported but never applied.

Output: A single font-family declaration added to the existing body rule in pwg.css.
Phase 3 (Typography & Token Completion) will later promote this to a CSS custom
property and assign heading fonts — this plan only closes the UAT gap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-broken-foundation-fixes/01-UAT.md

<interfaces>
<!-- Current body rule in css/pwg.css (lines 38-41): -->
body {
  background-color: var(--stone-mist);
  color: var(--stone-dark);
}

<!-- Google Fonts import in _includes/head.html (line 14): -->
<link href="https://fonts.googleapis.com/css2?family=Lato:wght@400;700&family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">

<!-- Bootstrap 3 body default (from bootstrap.min.css — do not edit): -->
/* font-family: "Helvetica Neue", Helvetica, Arial, sans-serif; */
/* Bootstrap sets this; pwg.css must override it via the body selector */
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply Lato font-family to body in pwg.css</name>
  <files>css/pwg.css</files>

  <read_first>
    Read css/pwg.css lines 36-42 to confirm the exact current body rule before editing.
    Command: rg -n "^body" css/pwg.css
  </read_first>

  <action>
    Edit the existing body rule in css/pwg.css (in the PAGE BASE section, currently lines
    38-41) to add a font-family declaration.

    The body rule must become:

    body {
      background-color: var(--stone-mist);
      color: var(--stone-dark);
      font-family: 'Lato', sans-serif; /* Google Fonts import in head.html (INFRA-01) */
    }

    Constraints:
    - Add ONLY the font-family line — do not change background-color or color.
    - Do NOT add font-family to h1/h2/h3 or any heading elements. Heading font
      assignments belong to Phase 3 (Typography & Token Completion). The h1/h2/h3
      rule in the PAGE CONTAINER POLISH section (sets color: var(--pine-deep)) must
      remain unchanged.
    - Do NOT create a new body rule — add the property to the existing one.
    - Do NOT add Open Sans anywhere yet. Phase 3 will decide the heading pairing.
    - The comment "(INFRA-01)" in the font-family line provides traceability to the
      requirement this gap closure satisfies.
  </action>

  <verify>
    <automated>rg -n "font-family" css/pwg.css</automated>
    Expected output contains exactly one match: the body rule showing
    `font-family: 'Lato', sans-serif;`
    No other font-family rules should exist in the file.

    Also confirm Bootstrap load order is untouched:
    <automated>rg -n "font-family" css/pwg.css | wc -l</automated>
    Must output: 1
  </verify>

  <acceptance_criteria>
    - `rg "font-family: 'Lato'" css/pwg.css` returns exactly one match in the body rule
    - `rg "font-family" css/pwg.css | wc -l` outputs 1 (no duplicate or heading rules)
    - The body rule still contains background-color and color properties (not removed)
    - No h1, h2, h3, or heading selector has a font-family property in pwg.css
    - pwg.css file size is larger than before (a line was added, not replaced)
  </acceptance_criteria>

  <done>
    css/pwg.css body rule contains font-family: 'Lato', sans-serif. When a browser
    loads any page, DevTools Network > Font tab shows Lato binary files downloading
    from fonts.gstatic.com. Body text no longer renders in Helvetica/Arial.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN → browser | Google Fonts stylesheet and font binaries load over HTTPS (already enforced by head.html) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | css/pwg.css body rule | accept | CSS file is static; served from GitHub Pages with no write path from the web |
| T-01-03-02 | Information Disclosure | Google Fonts CDN | accept | Font loading reveals page visits to Google; HOA has no PII concern with this — accepted at project start |
</threat_model>

<verification>
Manual check after execution:
1. Open any page locally (`bundle exec jekyll serve`)
2. Open DevTools > Network tab > filter by "Font"
3. Reload the page
4. Confirm requests appear for fonts.gstatic.com serving Lato-Regular and Lato-Bold
5. In DevTools > Elements, inspect `<body>`. Computed styles should show font-family
   starting with "Lato" (not "Helvetica Neue")

Automated check (no browser required):
- `rg "font-family: 'Lato'" css/pwg.css` must return exactly one match
</verification>

<success_criteria>
The body rule in css/pwg.css contains `font-family: 'Lato', sans-serif;` as its
third property. The Google Fonts CDN delivers Lato binary files when the page loads.
UAT test 2 passes: fonts.googleapis.com and fonts.gstatic.com appear in the DevTools
Network waterfall with no mixed-content warnings.
</success_criteria>

<output>
After completion, create `.planning/phases/01-broken-foundation-fixes/01-03-SUMMARY.md`
following the summary template at @$HOME/.claude/get-shit-done/templates/summary.md
</output>
