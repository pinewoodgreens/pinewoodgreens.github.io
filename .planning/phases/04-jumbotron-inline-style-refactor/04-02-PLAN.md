---
phase: 04-jumbotron-inline-style-refactor
plan: "02"
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - _includes/adsjumbotron.html
  - _includes/annualmeetingjumbotron.html
  - _includes/emailjumbotron.html
  - _includes/emailjumbotronold
  - _includes/pavingjumbotron.html
  - _includes/pooljumbotron.html
  - _includes/recyclingalert.html
  - _includes/snowjumbotron.html
  - _includes/specialeventjumbotron.html
  - _includes/trashchangejumbotron
  - _includes/volunteerjumbotron.html
  - index.html
  - newsarchive.md
  - subscribe.md
autonomous: true
requirements: [DSS-02, DSS-03, DSS-04]

must_haves:
  truths:
    - "No jumbotron include file contains a background-image inline style= attribute"
    - "No jumbotron container uses inline background-color rgba() values"
    - "No inline <style> block appears in index.html, newsarchive.md, or subscribe.md"
    - "All eleven jumbotron includes use CSS modifier classes defined in Plan 01"
  artifacts:
    - path: "_includes/volunteerjumbotron.html"
      provides: "Representative jumbotron — treecover background, 0.8 overlay"
      contains: "jumbotron--bg-treecover"
    - path: "_includes/pooljumbotron.html"
      provides: "Pool jumbotron — pool-texture background, 0.9 overlay"
      contains: "jumbotron--bg-pool"
    - path: "_includes/snowjumbotron.html"
      provides: "Snow jumbotron — snow.JPG background, 0.9 overlay"
      contains: "jumbotron--bg-snow"
    - path: "index.html"
      provides: "Homepage — inline <style> block removed"
  key_links:
    - from: "_includes/*jumbotron*"
      to: "css/pwg.css"
      via: "CSS modifier classes jumbotron--bg-* and jumbotron-overlay / jumbotron-overlay--deep"
      pattern: "jumbotron--bg-"
---

<objective>
Remove all inline styles from jumbotron includes and inline `<style>` blocks from HTML/Markdown pages, replacing them with the CSS classes defined in Plan 01.

Purpose: Completes DSS-02, DSS-03, DSS-04 — the token system now controls every visual aspect of jumbotrons and the migrated widget styles.
Output: 14 modified files with zero inline `style=` attributes on jumbotron elements and zero `<style>` blocks in page files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-jumbotron-inline-style-refactor/04-01-SUMMARY.md
</context>

<interfaces>
<!-- Class names defined in Plan 01 that this plan applies. -->

Background modifier classes (add to .jumbotron element):
  .jumbotron--bg-treecover   → url('/images/treecover.jpg')
  .jumbotron--bg-pool        → url('/images/pool-texture.jpg')
  .jumbotron--bg-snow        → url('/images/snow.JPG')
  .jumbotron--bg-emailsignup → url('/images/emailsignup.jpg')

Overlay classes (add to .container element nested inside .jumbotron):
  .jumbotron-overlay        → rgba(0,0,0,0.8)
  .jumbotron-overlay--deep  → rgba(0,0,0,0.9)

Typography classes (replace inline <style> blocks):
  .display_archive  → font-family + font-size for Mailchimp list widget
  .campaign         → line-height + margin for Mailchimp list widget

Current inline style patterns to remove:
  Jumbotron div:  style="background: url('images/treecover.jpg') 50% 0 no-repeat;"
  Container div:  style="background-color: rgba(0,0,0,0.8);"
  H1/H2 heading:  style="color: #fff;"   ← already handled by .whitetext class on <p>; h1/h2 need class="text-center whitetext"

Note on heading color: The inline style="color: #fff;" on h1/h2 elements inside jumbotrons must also be removed (DSS-02 scope). Replace with class="text-center whitetext" since .whitetext { color: #FFF; } already exists in pwg.css (line 121–123).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refactor all eleven jumbotron include files</name>
  <files>
    _includes/adsjumbotron.html,
    _includes/annualmeetingjumbotron.html,
    _includes/emailjumbotron.html,
    _includes/emailjumbotronold,
    _includes/pavingjumbotron.html,
    _includes/pooljumbotron.html,
    _includes/recyclingalert.html,
    _includes/snowjumbotron.html,
    _includes/specialeventjumbotron.html,
    _includes/trashchangejumbotron,
    _includes/volunteerjumbotron.html
  </files>
  <read_first>
    _includes/adsjumbotron.html,
    _includes/annualmeetingjumbotron.html,
    _includes/emailjumbotron.html,
    _includes/emailjumbotronold,
    _includes/pavingjumbotron.html,
    _includes/pooljumbotron.html,
    _includes/recyclingalert.html,
    _includes/snowjumbotron.html,
    _includes/specialeventjumbotron.html,
    _includes/trashchangejumbotron,
    _includes/volunteerjumbotron.html
  </read_first>
  <action>
For each of the eleven jumbotron files, apply the following transformations. Read each file before editing.

BACKGROUND IMAGE RULE (apply to every file):
  Remove the inline style="background: url('images/X.jpg') ..." attribute from the opening .jumbotron div.
  Add the corresponding CSS modifier class to the class= attribute of that div.

  Image → class mapping:
    images/treecover.jpg    → jumbotron--bg-treecover
    images/pool-texture.jpg → jumbotron--bg-pool
    images/snow.JPG         → jumbotron--bg-snow
    images/emailsignup.jpg  → jumbotron--bg-emailsignup

  Before: <div class="jumbotron" style="background: url('images/treecover.jpg') 50% 0 no-repeat;">
  After:  <div class="jumbotron jumbotron--bg-treecover">

OVERLAY RULE (apply to every file):
  Remove the inline style="background-color: rgba(0,0,0,X);" attribute from the .container div nested inside the jumbotron.
  Add the corresponding CSS class to that div's class= attribute.

  rgba value → class mapping:
    rgba(0,0,0,0.8) → jumbotron-overlay
    rgba(0,0,0,0.9) → jumbotron-overlay--deep

  Before: <div class="container" style="background-color: rgba(0,0,0,0.8);">
  After:  <div class="container jumbotron-overlay">

  Before: <div class="container" style="background-color: rgba(0,0,0,0.9);">
  After:  <div class="container jumbotron-overlay--deep">

HEADING COLOR RULE (apply to h1 and h2 elements with style="color: #fff;"):
  Remove the inline style="color: #fff;" attribute.
  Add "whitetext" to the element's class= attribute (keep "text-center" if present).

  Before: <h1 class="text-center" style="color: #fff;">Welcome Home</h1>
  After:  <h1 class="text-center whitetext">Welcome Home</h1>

  Before: <h2 class="text-center" style="color: #fff;">Some Title</h2>
  After:  <h2 class="text-center whitetext">Some Title</h2>

Apply all three rules to each file. Do not change any other content (text, links, button hrefs, or paragraph content).

Per-file overlay class reference (from the scan in Plan 01):
  adsjumbotron.html           → jumbotron--bg-treecover, jumbotron-overlay (0.8)
  annualmeetingjumbotron.html → jumbotron--bg-treecover, jumbotron-overlay (0.8)
  emailjumbotron.html         → jumbotron--bg-treecover, jumbotron-overlay--deep (0.9)
  emailjumbotronold           → jumbotron--bg-emailsignup, jumbotron-overlay--deep (0.9)
  pavingjumbotron.html        → jumbotron--bg-treecover, jumbotron-overlay (0.8)
  pooljumbotron.html          → jumbotron--bg-pool, jumbotron-overlay--deep (0.9)
  recyclingalert.html         → jumbotron--bg-treecover, jumbotron-overlay (0.8)
  snowjumbotron.html          → jumbotron--bg-snow, jumbotron-overlay--deep (0.9)
  specialeventjumbotron.html  → jumbotron--bg-treecover, jumbotron-overlay (0.8)
  trashchangejumbotron        → jumbotron--bg-treecover, jumbotron-overlay (0.8)
  volunteerjumbotron.html     → jumbotron--bg-treecover, jumbotron-overlay (0.8)
  </action>
  <verify>
    <automated>grep -rl "background: url\|background-color: rgba" /Users/nwmaynes/Documents/code-projects/pinewoodgreens.github.io/_includes/ | grep -i "jumbotron\|trashchange"</automated>
  </verify>
  <acceptance_criteria>
    - grep -rl "background: url" _includes/ | grep -i "jumbotron" returns empty (no matches)
    - grep -rl "rgba(0,0,0" _includes/ | grep -i "jumbotron\|trashchange" returns empty
    - grep "jumbotron--bg-treecover" _includes/volunteerjumbotron.html returns 1 match
    - grep "jumbotron-overlay" _includes/volunteerjumbotron.html returns 1 match
    - grep "style=" _includes/volunteerjumbotron.html returns empty (no inline styles remain)
    - grep "jumbotron--bg-pool" _includes/pooljumbotron.html returns 1 match
    - grep "jumbotron-overlay--deep" _includes/pooljumbotron.html returns 1 match
    - grep "jumbotron--bg-snow" _includes/snowjumbotron.html returns 1 match
    - grep "jumbotron--bg-emailsignup" _includes/emailjumbotronold returns 1 match
    - grep "whitetext" _includes/volunteerjumbotron.html returns 1 match (on h1 element)
    - grep 'style="color: #fff' _includes/volunteerjumbotron.html returns empty
  </acceptance_criteria>
  <done>All eleven jumbotron includes have zero inline style= attributes on their .jumbotron and .container elements. Heading color inline styles replaced with whitetext class. CSS modifier classes correctly applied per the per-file reference table.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove inline &lt;style&gt; blocks from index.html, newsarchive.md, subscribe.md</name>
  <files>index.html, newsarchive.md, subscribe.md</files>
  <read_first>index.html, newsarchive.md, subscribe.md</read_first>
  <action>
Read each file first to confirm current state, then apply these targeted edits.

--- index.html ---
Locate lines 41–46 (the inline <style> block inside the "Neighborhood News" panel):
```
<style type="text/css">
<!--
.display_archive {font-family: helvetica,sans-serif; font-size: 14px;}
.campaign {line-height: 125%; margin: 5px;}
//-->
</style>
```
Delete these six lines entirely. The .display_archive and .campaign classes are now defined in pwg.css (added in Plan 01). The Mailchimp script tag immediately below the style block must remain.

--- newsarchive.md ---
Locate lines 6–11 (the inline <style> block):
```
<style type="text/css">
<!--
.display_archive {font-family: arial,verdana; font-size: 12px;}
.campaign {line-height: 125%; margin: 5px;}
//-->
</style>
```
Delete these six lines entirely. The classes are now in pwg.css. The Mailchimp script tag immediately below must remain.

--- subscribe.md ---
Locate lines 10–14 (the inline <style> block):
```
  <style type="text/css">
        #mc_embed_signup{background:#fff; false;clear:left; font:14px Helvetica,Arial,sans-serif; width: 600px;}
        /* Add your own Mailchimp form style overrides in your site stylesheet or in this style block.
           We recommend moving this block and the preceding CSS link to the HEAD of your HTML file. */
</style>
```
Delete these five lines entirely. The #mc_embed_signup background, font, and width rules have been migrated to pwg.css in Plan 01. Do NOT remove any other content in subscribe.md — keep the form HTML, script tags, and surrounding paragraph text intact.

Do not change any other lines in any of these three files.
  </action>
  <verify>
    <automated>grep -n "<style" /Users/nwmaynes/Documents/code-projects/pinewoodgreens.github.io/index.html /Users/nwmaynes/Documents/code-projects/pinewoodgreens.github.io/newsarchive.md /Users/nwmaynes/Documents/code-projects/pinewoodgreens.github.io/subscribe.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "<style" index.html returns empty (no matches)
    - grep "<style" newsarchive.md returns empty (no matches)
    - grep "<style" subscribe.md returns empty (no matches)
    - grep "generate-js" index.html returns 1 match (Mailchimp script not accidentally removed)
    - grep "generate-js" newsarchive.md returns 1 match (Mailchimp script not accidentally removed)
    - grep "mc-embedded-subscribe-form" subscribe.md returns 1 match (form HTML intact)
    - grep "mc-validate.js" subscribe.md returns 1 match (Mailchimp validate script intact)
  </acceptance_criteria>
  <done>Zero &lt;style&gt; tags remain in index.html, newsarchive.md, and subscribe.md. All Mailchimp script tags and form HTML are intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML/Markdown edit | Template fragments and Markdown with embedded HTML — risk is breaking Jekyll includes or Mailchimp widget |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | Mailchimp script tags | mitigate | acceptance_criteria verify generate-js and mc-validate.js survive; read each file before editing |
| T-04-05 | Denial of Service | jumbotron visual breakage | mitigate | CSS classes must be defined in pwg.css (Plan 01) before this plan runs; wave dependency enforces this |
| T-04-06 | Tampering | Jumbotron text / links | accept | Only class= and style= attributes are changed; inner text content, href values, and button labels are untouched |
| T-04-07 | Information Disclosure | emailjumbotronold (no .html extension) | accept | File is an internal Jekyll include, not publicly routed; no exposure risk |
</threat_model>

<verification>
After both tasks complete:

```bash
# No background-image inline styles remain in jumbotron includes
grep -rl "background: url" _includes/ | grep -i "jumbotron\|trashchange"

# No rgba overlay inline styles remain
grep -rl "rgba(0,0,0" _includes/ | grep -i "jumbotron\|trashchange"

# No <style> blocks in page files
grep -c "<style" index.html newsarchive.md subscribe.md

# CSS classes applied
grep "jumbotron--bg-treecover" _includes/volunteerjumbotron.html
grep "jumbotron-overlay--deep" _includes/pooljumbotron.html

# Critical Mailchimp assets intact
grep "generate-js" index.html newsarchive.md
grep "mc-validate.js" subscribe.md
```
</verification>

<success_criteria>
- `grep -rl "background: url" _includes/ | grep -i "jumbotron\|trashchange"` returns empty
- `grep -rl "rgba(0,0,0" _includes/ | grep -i "jumbotron\|trashchange"` returns empty
- `grep -c "<style" index.html newsarchive.md subscribe.md` returns 0:0:0
- All eleven jumbotron files contain the correct CSS modifier class
- Mailchimp script tags in all three page files remain intact
</success_criteria>

<output>
After completion, create `.planning/phases/04-jumbotron-inline-style-refactor/04-02-SUMMARY.md`
</output>
