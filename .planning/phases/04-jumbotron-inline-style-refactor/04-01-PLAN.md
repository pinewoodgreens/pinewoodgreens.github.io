---
phase: 04-jumbotron-inline-style-refactor
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - css/pwg.css
autonomous: true
requirements: [DSS-01, DSS-02, DSS-03, DSS-04]

must_haves:
  truths:
    - "pwg.css :root block contains an inline comment on --pine-sage warning it must never be used as text color"
    - "pwg.css defines CSS modifier classes for every jumbotron background-image variant (treecover, pool-texture, snow, emailsignup)"
    - "pwg.css defines CSS classes for both jumbotron overlay opacity variants (.jumbotron-overlay and .jumbotron-overlay--deep)"
    - "pwg.css defines .display_archive and .campaign rules so inline <style> blocks in HTML/Markdown can be removed"
  artifacts:
    - path: "css/pwg.css"
      provides: "All new CSS rules required by Plans 02 and 03"
      contains: "--pine-sage"
  key_links:
    - from: "css/pwg.css"
      to: "_includes/*jumbotron*.html"
      via: "CSS modifier class selectors applied in Plan 02"
      pattern: "jumbotron--bg-"
---

<objective>
Add all CSS rules that Plans 02 and 03 depend on before any HTML or Markdown is touched.

Purpose: Plan 02 removes inline styles by replacing them with class names — those classes must exist in pwg.css first or the site will break between commits.
Output: Extended pwg.css with four new rule groups: --pine-sage safety comment, jumbotron background modifier classes, jumbotron overlay opacity classes, and Mailchimp list typography classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-typography-token-completion/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Current pwg.css token values the executor needs. Extracted from css/pwg.css. -->

From css/pwg.css :root block (lines 10–36):
```css
--pine-deep:    #1A3820;
--pine-dark:    #2B5230;
--pine-main:    #3D6B43;
--pine-sage:    #5C8060;   /*  3.9:1 on white — hover, secondary UI */
--pine-light:   #9BB89E;
--pine-mist:    #EAF3EB;
--font-body:    'Lato', 'Helvetica Neue', Helvetica, Arial, sans-serif;
--font-heading: 'Merriweather', Georgia, 'Times New Roman', serif;
```

From css/pwg.css JUMBOTRON section (lines 190–198):
```css
.jumbotron {
  background-color: var(--pine-mist);
  border-left: 4px solid var(--pine-light);
}
.jumbotron h1,
.jumbotron h2 {
  color: var(--pine-deep);
}
```

Inline styles being replaced (sourced from _includes/ scan):
- background: url('images/treecover.jpg') 50% 0 no-repeat;   (8 files)
- background: url('images/pool-texture.jpg') 50% 0 no-repeat; (1 file)
- background: url('images/snow.JPG') 50% 0 no-repeat;         (1 file)
- background: url('images/emailsignup.jpg') 50% 0 no-repeat;  (1 file: emailjumbotronold)
- background-color: rgba(0,0,0,0.8)  (overlay class: .jumbotron-overlay)
- background-color: rgba(0,0,0,0.9)  (overlay class: .jumbotron-overlay--deep)

Inline <style> content being migrated from index.html, newsarchive.md:
  .display_archive {font-family: arial,verdana; font-size: 12px;}
  .campaign {line-height: 125%; margin: 5px;}

Inline <style> content being migrated from subscribe.md (width rule already handled by INFRA-04 in pwg.css):
  #mc_embed_signup{background:#fff; false;clear:left; font:14px Helvetica,Arial,sans-serif; width: 600px;}
  Note: "false;" is a syntax error in the original — omit it. Width 600px overridden by INFRA-04 rule already in pwg.css.
  The font stack conflicts with TYPE-02; use var(--font-body) instead of hardcoded Helvetica.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add --pine-sage safety comment to :root</name>
  <files>css/pwg.css</files>
  <read_first>css/pwg.css</read_first>
  <action>
In css/pwg.css, locate the --pine-sage token line inside the :root block (currently line 15):

  --pine-sage:    #5C8060;   /*  3.9:1 on white — hover, secondary UI */

Replace it with:

  --pine-sage:    #5C8060;   /* 3.9:1 on white — hover, secondary UI ONLY.
                                NEVER use as text color: fails WCAG AA at 3.9:1 (minimum 4.5:1 required). */

Keep indentation consistent with surrounding token lines.
  </action>
  <verify>
    <automated>grep -n "NEVER use as text color" /Users/nwmaynes/Documents/code-projects/pinewoodgreens.github.io/css/pwg.css</automated>
  </verify>
  <acceptance_criteria>
    - grep "NEVER use as text color" css/pwg.css returns a match on the --pine-sage line
    - grep "pine-sage" css/pwg.css shows the token is still present and unchanged (#5C8060)
  </acceptance_criteria>
  <done>--pine-sage token has an inline comment containing the exact phrase "NEVER use as text color" in the :root block of pwg.css</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add jumbotron background modifier classes and overlay classes to pwg.css</name>
  <files>css/pwg.css</files>
  <read_first>css/pwg.css</read_first>
  <action>
In css/pwg.css, locate the existing JUMBOTRON section (lines ~190–223, ending after the HERO ROW @media block). Immediately after the closing brace of the HERO ROW @media block, add the following new rule groups. Do NOT modify existing rules.

Append after line ~223 (after the hero-row @media closing brace):

```css
/* --------------------------------------------------------------------------
   JUMBOTRON BACKGROUND MODIFIER CLASSES — DSS-02
   Replaces inline style="background: url(...)" on .jumbotron elements.
   All images use 50% 0 no-repeat positioning to match original layout.
   -------------------------------------------------------------------------- */
.jumbotron--bg-treecover {
  background-image: url('/images/treecover.jpg');
  background-position: 50% 0;
  background-repeat: no-repeat;
  background-size: cover;
}

.jumbotron--bg-pool {
  background-image: url('/images/pool-texture.jpg');
  background-position: 50% 0;
  background-repeat: no-repeat;
  background-size: cover;
}

.jumbotron--bg-snow {
  background-image: url('/images/snow.JPG');
  background-position: 50% 0;
  background-repeat: no-repeat;
  background-size: cover;
}

.jumbotron--bg-emailsignup {
  background-image: url('/images/emailsignup.jpg');
  background-position: 50% 0;
  background-repeat: no-repeat;
  background-size: cover;
}

/* When a background image is present, override the default mint-tint background */
.jumbotron[class*="jumbotron--bg-"] {
  background-color: transparent;
}

/* --------------------------------------------------------------------------
   JUMBOTRON OVERLAY CLASSES — DSS-03
   Replaces inline style="background-color: rgba(0,0,0,X)" on .container
   elements nested inside jumbotrons.
   Two variants match the two opacity values in use across the includes.
   -------------------------------------------------------------------------- */
.jumbotron-overlay {
  background-color: rgba(0, 0, 0, 0.8);
}

.jumbotron-overlay--deep {
  background-color: rgba(0, 0, 0, 0.9);
}
```

Then append the Mailchimp list classes at the end of the MAILCHIMP EMBED section (after line ~297):

```css
/* Mailchimp newsletter list widget — DSS-04
   Migrated from inline <style> in index.html and newsarchive.md.
   font-family uses var(--font-body) rather than the original hardcoded
   "arial,verdana" to stay consistent with TYPE-02. */
.display_archive {
  font-family: var(--font-body);
  font-size: 0.75rem; /* 12px equivalent in rem — faithful migration of original 12px */
}

.campaign {
  line-height: 1.25;
  margin: 5px 0;
}
```

Then append the mc_embed_signup font override at the end of the existing #mc_embed_signup block:

```css
/* Mailchimp signup form base — DSS-04
   Migrated from inline <style> in subscribe.md.
   background-color and width already handled by INFRA-04 rules above.
   font replaced with var(--font-body) for TYPE-02 compliance. */
#mc_embed_signup {
  font-family: var(--font-body);
  font-size: 0.875rem;
  background-color: #fff;
}
```

Note: The existing #mc_embed_signup rule (width: 100% !important; max-width: 600px;) was added in INFRA-04 and must remain intact. Add the new font/bg rule as a second #mc_embed_signup rule block below it — CSS cascade will merge them correctly.
  </action>
  <verify>
    <automated>grep -c "jumbotron--bg-treecover\|jumbotron--bg-pool\|jumbotron--bg-snow\|jumbotron-overlay\|display_archive\|\.campaign" /Users/nwmaynes/Documents/code-projects/pinewoodgreens.github.io/css/pwg.css</automated>
  </verify>
  <acceptance_criteria>
    - grep "jumbotron--bg-treecover" css/pwg.css returns 1 match
    - grep "jumbotron--bg-pool" css/pwg.css returns 1 match
    - grep "jumbotron--bg-snow" css/pwg.css returns 1 match
    - grep "jumbotron--bg-emailsignup" css/pwg.css returns 1 match
    - grep "jumbotron-overlay--deep" css/pwg.css returns 1 match
    - grep "\.jumbotron-overlay {" css/pwg.css returns 1 match
    - grep "\.display_archive" css/pwg.css returns 1 match
    - grep "\.campaign {" css/pwg.css returns 1 match
    - The existing ".jumbotron {" rule (background-color: var(--pine-mist)) is unchanged — grep ".jumbotron {" css/pwg.css returns 1 match
    - grep "width: 100% !important" css/pwg.css still returns 1 match (INFRA-04 rule not removed)
  </acceptance_criteria>
  <done>pwg.css contains all four background modifier classes, both overlay classes, .display_archive, .campaign, and the mc_embed_signup font override. No existing rules are modified or removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS file edit | Plain text file — no execution surface; risk is style regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | css/pwg.css | mitigate | Read file before editing; do not remove existing INFRA-04 or Phase 2 rules; acceptance_criteria verify their survival |
| T-04-02 | Denial of Service | .jumbotron background-color override | mitigate | Use attribute selector `[class*="jumbotron--bg-"]` to only suppress mint tint when a bg image class is present; plain .jumbotron retains default styling |
| T-04-03 | Tampering | rgba overlay values | accept | Values are decorative (0.8 / 0.9 black overlay); no PII, no auth; acceptance_criteria confirm exact values |
</threat_model>

<verification>
After both tasks complete, run:

```bash
grep -n "NEVER use as text color" css/pwg.css
grep -c "jumbotron--bg-" css/pwg.css        # expect 5 (4 classes + 1 attribute selector)
grep -c "jumbotron-overlay" css/pwg.css      # expect 2
grep "\.display_archive" css/pwg.css
grep "width: 100% !important" css/pwg.css    # INFRA-04 rule must still exist
```
</verification>

<success_criteria>
- pwg.css :root --pine-sage line contains the exact phrase "NEVER use as text color"
- Four jumbotron background modifier classes exist (.jumbotron--bg-treecover, --bg-pool, --bg-snow, --bg-emailsignup)
- Two overlay classes exist (.jumbotron-overlay with 0.8, .jumbotron-overlay--deep with 0.9)
- .display_archive and .campaign rules defined
- #mc_embed_signup font/background rules added
- All pre-existing pwg.css rules survive intact (verified by grep)
</success_criteria>

<output>
After completion, create `.planning/phases/04-jumbotron-inline-style-refactor/04-01-SUMMARY.md`
</output>
